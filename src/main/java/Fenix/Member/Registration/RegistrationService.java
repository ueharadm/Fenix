package Fenix.Member.Registration;

import Fenix.Member.Member;
import Fenix.Member.MemberRepository;
import Fenix.Member.MemberService;
import lombok.AllArgsConstructor;

@AllArgsConstructor
public class RegistrationService {
/*
	private static final MemberService memberService = new MemberService();
	private static final EmailValidator emailValidator= new EmailValidator();
	
	public Member register(String name, String phoneNumber, int id, String cpf, int degree, int lodge) {
		return memberService.signUpMember(
				new Member(
						name,
						phoneNumber,
						id,
						cpf,
						degree,
						lodge)
				);
	}
*/
}
